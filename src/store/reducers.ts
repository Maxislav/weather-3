import { Action, AnyAction, combineReducers } from "redux";import { OpenweathermapItem } from "../interfaces/openweathermap";export interface ApiOpenWeatherState{    loading: boolean;    loaded: boolean;    list: OpenweathermapItem[]}const defaultState: ApiOpenWeatherState = {    loading: false,    loaded: false,    list: []}export const apiOpenWeather = (state = defaultState, action: AnyAction) => {    switch (action.type){        case "LOADING_START": {            return {                ...state,                loading: true,                list: [],            }        }        case "LOADING_SUCCESS": {            return {                ...state,                loading: false,                loaded: true,                list: action.payload            }        }        default: {            return state        }    }}export const rootReducer = combineReducers({    apiOpenWeather})