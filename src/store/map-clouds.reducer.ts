import { AnyAction } from "redux";export interface MapCloudsState{    loading: boolean,    loaded: boolean,    list: any[],}const defaultState: MapCloudsState = {    loading: false,    loaded: false,    list: []}export const mapCloudsStateReducer = (state = defaultState, action: AnyAction) => {    switch (action.type){        default: {            return state        }    }}