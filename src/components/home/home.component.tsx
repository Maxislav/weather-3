import { useLoaderData } from "react-router-dom";import { useState } from "react";import styles from './home.component.less'import { ImagesComponent } from "../images/images.component";interface HomeLoaderData {    date: string;}export function sleep(n: number = 500) {    return new Promise((r) => setTimeout(r, n));}export async function homeLoader(): Promise<{date: string}> {    await sleep();    return {        date: new Date().toISOString(),    };}export function Home(s: any) {    let data = useLoaderData() as HomeLoaderData;    const [count, setCount] = useState(0);    async function presetCount(e: any){        await sleep()        setCount(count + 1)    }    return (        <div className={styles.container}>            <div >                <ImagesComponent></ImagesComponent>            </div>            <div>            </div>        </div>    );}