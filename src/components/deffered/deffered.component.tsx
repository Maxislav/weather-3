import { useLoaderData } from "react-router-dom";import styles from './deffered.component.less'function sleep(n: number = 20) {    return new Promise((r) => setTimeout(r, n));}export async function deferredLoader(): Promise<{date: string}> {    await sleep();    return {        date: new Date().toISOString(),    };}export function DeferredPage(){   // let data = {date: 'gigi'};    let data = useLoaderData() as {date: string};    return (        <>            <h2>DeferredPage</h2>            <div className={styles.container}>                <div className={styles.el}>                        1 dssssssc                </div>            </div>            <div className={styles.container}>                <div className={styles.el}>                    2 dsd                </div>                <div className={styles.el}>                    3 dsaaaaa                </div>                <div className={styles.el}>                    4 dsaaaaa                </div>            </div>            <p>Date from loader: {data.date}</p>        </>    );}