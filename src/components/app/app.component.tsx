import { HashRouter, RouterProvider } from "react-router-dom";import { myRouter } from "../../router/browser.routes";import styles from './app.component.less'import { Fallback } from "../fallback/fallback.component";import { Provider } from "react-redux";import { store } from "../../store/main-store.store";//HashRouterProviexport function App() {    return <>        <Provider store={ store }>            <div className={ styles.app }>                <RouterProvider router={ myRouter } fallbackElement={ <Fallback/> }/>            </div>        </Provider>    </>}