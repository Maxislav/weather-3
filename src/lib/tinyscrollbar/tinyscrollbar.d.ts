declare class Plugin {    constructor($container: HTMLElement, options: TinyOpts)    update(scrollTo?: number | string): Plugin}interface TinyOpts {    axis?: string; // = 'y' – Vertical or horizontal scroller? ( x || y ).    wheel?: boolean;// = true – Enable or disable the mousewheel.    wheelSpeed?: boolean; // = 40 – How many pixels must the mousewheel scroll at a time.    wheelLock?: boolean; // = true – Lock default window wheel scrolling when there is no more content to scroll.    touchLock?: number; // = true – Lock default window touch scrolling when there is no more content to scroll.    trackSize?: boolean | number; // = false – Set the size of the scrollbar to auto(false) or a fixed number.    thumbSize?: boolean | number; // = false – Set the size of the thumb to auto(false) or a fixed number    thumbSizeMin?: boolean;// = 20 – Minimum thumb size.}function tinyscrollbar($container: HTMLElement, options: TinyOpts): Pluginexport default tinyscrollbar