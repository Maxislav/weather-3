import express from 'express'import path from "path";import * as fs from "fs";import ReactDOMServer  from 'react-dom/server'import {SingleTestComponent} from '../src/components/single-test/single-test.component'const PORT = 3777const app = express()/*app.get('/', (req, res) => {    res.send('Hello World dd!')})*/app.get( /\.(js|css|map|ico)$/, express.static( path.resolve( __dirname, '../build' ) ) );app.use( '*', ( req, res ) => {// читаем файл `index.html`    let indexHTML = fs.readFileSync( path.resolve( __dirname, '../build/index.html' ), {        encoding: 'utf8',    } );    let appHTML = ReactDOMServer.renderToString( <SingleTestComponent/> );// заполняем элемент '#app' содержимым из 'appHTML'    indexHTML = indexHTML.replace( '<div id="root"></div>',  `<div id="app">${ appHTML }</div>`);// устанавливаем заголовок и статус    res.contentType( 'text/html' );    res.status( 200 );    return res.send( indexHTML );} );console.log('server starting on port', PORT)app.listen(PORT, () => {    console.log(`Example app listening on port ${ PORT }`)})